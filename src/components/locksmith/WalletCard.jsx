import React from "react";
import { Wallet, ArrowDownToLine, Clock } from "lucide-react";

export default function WalletCard({ balance, pending, onWithdraw }) {
  if (Number(balance) <= 0 && Number(pending) <= 0) return null;
  return <div className="p-5 rounded-2xl bg-gradient-to-br from-emerald-600 to-emerald-700 text-white"><div className="flex items-center gap-2 mb-2"><Wallet className="w-5 h-5" /><span className="text-sm font-medium opacity-90">Saldo interno legado</span></div><p className="font-heading font-bold text-3xl">R$ {(Number(balance) || 0).toFixed(2)}</p><div className="flex items-center gap-1.5 text-xs opacity-80 mt-1"><Clock className="w-3.5 h-3.5" /><span>R$ {(Number(pending) || 0).toFixed(2)} em saque solicitado</span></div><p className="text-xs mt-2 opacity-90">Novas cobranças usam o Mercado Pago conectado ou geram créditos pendentes na plataforma, separados deste saldo legado.</p>{onWithdraw && <button onClick={onWithdraw} disabled={!balance || balance <= 0} className="w-full mt-3 flex items-center justify-center gap-2 p-3 rounded-xl bg-white/20 text-sm font-medium disabled:opacity-50"><ArrowDownToLine className="w-4 h-4" /> Solicitar saldo legado via Pix</button>}</div>;
}