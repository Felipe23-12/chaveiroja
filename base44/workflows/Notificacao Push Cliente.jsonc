{
  "name": "Notifica\u00e7\u00e3o Push Cliente",
  "description": "Envia notifica\u00e7\u00e3o push ao celular do cliente a cada etapa do chamado (aceito, a caminho, chegou, finalizado, conclu\u00eddo, cancelado), mesmo com o app fechado",
  "trigger": {
    "config": {
      "trigger_type": "entity",
      "entity_name": "ServiceRequest",
      "events": [
        "update"
      ]
    },
    "condition": "${ ((.trigger.changed_fields // []) | index(\"status\") != null or index(\"locksmith_arrived\") != null or index(\"end_photos\") != null) }"
  },
  "definition": {
    "document": {
      "dsl": "1.0.0",
      "name": "push_cliente_andamento",
      "version": "1.0",
      "namespace": "base44"
    },
    "do": [
      {
        "notificar_cliente": {
          "call": "invoke_backend_function",
          "with": {
            "function_name": "sendClientPushNotification",
            "args": {
              "service_request_id": "${ .trigger.entity_id }",
              "_internal_token": "cj_wf_int_7d2e5a1f9b8c4e30"
            }
          },
          "then": "end",
          "x-base44": {
            "title": "Notificar cliente",
            "description": "Push da etapa atual do chamado"
          }
        }
      }
    ]
  }
}